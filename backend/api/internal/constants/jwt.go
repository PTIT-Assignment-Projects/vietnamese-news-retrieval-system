package constants

const (
	JwtSecret                    = "JWT_SECRET"
	UnexpectedSigningMethodError = "unexpected signing method"
	InvalidToken                 = "invalid token"
	InvalidTokenClaims           = "invalid token claims"
)
